---
type Props = {
  id: string;
  bwSrc: string;
  colorSrc: string;
};

const { id, bwSrc, colorSrc } = Astro.props;
---

<style>
  .panel {
    position: absolute;
    inset: 0;
    height: 100dvh;
    z-index: -1;
  }
  .panel::before {
    content: "";
    position: fixed;
    inset: 0;
    background-image: var(--bw-img);
    opacity: 0.1;
    pointer-events: none;
  }
  .reveal-layer {
    position: fixed;
    inset: 0;
    background-color: var(--background-color, transparent);
    mask-image: url("#blob-mask");
    -webkit-mask-image: url("#blob-mask");
  }
  .reveal-layer::before {
    content: "";
    position: fixed;
    inset: 0;
    pointer-events: none;
    background-image: var(--color-img);
    opacity: 0.5;
    filter: saturate(1.5) contrast(1.5);
  }

  .background::before {
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center;
    background-attachment: fixed;
    transition: background-image 0.5s ease-in-out;
    will-change: background-image;
  }

  @media (prefers-color-scheme: dark) {
    .panel::before {
      opacity: 0.15;
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .reveal-layer {
      display: none;
    }
  }
</style>

<div
  id={id}
  class="panel background"
  style={`
    --bw-img: url(${bwSrc});
    --color-img: url(${colorSrc});
`}
>
  <div class="reveal-layer background" aria-hidden="true"></div>
  <slot />
</div>
